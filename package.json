{
  "name": "verification-fixture",
  "version": "0.1.0",
  "private": true,
  "description": "EZiL Works verification fixture: a tiny module with one intentionally failing test, used to prove the sandbox runner clones, installs, checks and tests a real repository.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx --bun tsc --noEmit -p tsconfig.json",
    "build": "echo build ok"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.0"
  }
}
